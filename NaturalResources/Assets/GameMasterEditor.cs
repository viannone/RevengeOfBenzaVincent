/*
using UnityEditor;
using UnityEngine;

[CustomEditor(typeof(GameMaster))]
class GameMasterEditor : Editor {
	public override void OnInspectorGUI() {
		DrawDefaultInspector ();
		if(GUILayout.Button("Reset"))
			PlayerPrefs.SetInt("Attempt", 0);
	}
}

*/